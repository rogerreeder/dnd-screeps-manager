/// <reference path="_references.ts" />

/**
 * My SCREEPS Manager
 * Runs the whole show
 */
class SCREEPSManager {
    static inject = [];
    constructor(
    
    ){

    }
    main() {
        //kick all the individual Managers....
        console.log('tick: ' + Game.time.toString());
    }
}